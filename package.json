{
  "name": "prototype-shoshana-dror",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "three": "^0.179.1",
    "vite": "^7.1.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "playwright": "^1.61.0",
    "wrangler": "^4.103.0"
  }
}
